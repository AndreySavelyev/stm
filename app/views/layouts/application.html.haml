!!!
%html
  %head
    %title Simple task manager
    = stylesheet_link_tag    "application"
    = stylesheet_link_tag "http://twitter.github.com/bootstrap/1.4.0/bootstrap.min.css"
    = javascript_include_tag "application"
    = csrf_meta_tags

  %body.container
  
    %div.flash
      - flash.each do |name, message|
        =content_tag :div, message, :id => "flash#{name}"
    %br/
    %div.user_nav
      -if current_user
        =t('user_nav.header')
        =current_user.email
        |
        =link_to t('user_nav.logout'), logout_path
      -else
        =link_to t('user_nav.signup'), signup_path
        or 
        =link_to t('user_nav.login'), login_path

    =yield